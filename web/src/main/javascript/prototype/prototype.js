define([
    "jquery",
    "underscore"],
    function ($, _) {
        $('body').append('<p>Prototype</p>');
        $('body').append('<p>' + window.location + '</p>');
        $('body').append('<p>bbbb</p>');
    });
